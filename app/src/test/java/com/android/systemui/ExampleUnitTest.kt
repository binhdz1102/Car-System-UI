package com.android.systemui

import junit.framework.TestCase.assertEquals
import org.junit.Test

/** Basic JVM smoke test for the SystemUI application module. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
